# DSH Mario Pixel Skin 维护约定

本仓库只负责 DeepSeek Harness 的浏览器视觉皮肤，不拥有 Harness 的会话、工作区、模型、权限、工具调用、设置或持久化行为。维护工作的成功标准不是“页面看起来变了”，而是皮肤加载后仍保留全部原生交互和无障碍语义，卸载时能撤销自己的每一项 DOM 与样式写入，并且在目标 DSH 版本更新后可以明确判断稳定层是否仍然成立、兼容层是否需要替换。

## 架构边界

`src/client.js` 是浏览器生命周期边界，只能创建皮肤自有的背景、标题栏、作用域属性和 CSS 变量。新增写入时必须在同一个 `ctx.effect()` disposer 中逐项撤销，不得修改 `document.title`，不得访问网络、Cookie、localStorage 或 sessionStorage，也不得复制、包裹或替换 Harness 的业务 DOM。`src/index.js` 是 Host 入口和 Schemastery 配置契约；配置默认值必须与 `src/client.js` 的边界默认值保持一致，因为 rc.6 的外部浏览器加载器可能只调用 `apply(ctx)`。

`src/styles/` 是稳定主题层，只能依赖 DSH 设计令牌、HTML 语义、公开 role、`data-pane` 与公开测试标记。这里不得出现 CSS-module 类名选择器或 `aria-label` 文案选择器。`src/compat/` 是版本适配层，只有这里可以使用已经从目标 DSH 静态产物或运行时 DOM 证实的私有类名和本地化标签；每次增加一个选择器，都要说明它修复的具体界面语义，并为该边界补充契约测试。禁止使用 `!important`，因为它会掩盖层叠关系、破坏试穿/卸载，并把后续升级问题变成无法定位的全局覆盖。

`scripts/build.mjs` 只负责按固定顺序合并样式、内联一份背景资源并生成 DSH 客户端模块。`lib/` 是生成产物，任何人都不得直接编辑。背景原图保留在 `assets/`，npm 包只分发 `lib/client.js` 内联的一份 WebP，避免重复体积。`skin.json`、`cordis.patch.yml`、`package.json` 和 README 中的包名、兼容版本与入口信息必须同步。

## 视觉判断原则

像素风来自清晰的边缘、有限色板、离散阴影和有意义的层级，不等于给所有元素套同一种粗框。静态标题应保持无按钮感；普通链接、文件、次要动作、主要动作和危险动作必须具有不同的形状、色彩或按压反馈。当前会话只使用较淡的底色和局部强调，不能与插件栏或主操作按钮等重。背景可以被看见，但正文工作面必须用半透明实色与模糊保证对比度，输入文字不得带 text-shadow 或描边。

设置入口属于固定的 `sidebar.settings` 席位，必须始终位于侧栏装饰之上。入口采用纸面状态栏按钮，齿轮图标使用蓝色徽章；设置弹窗中的导航行不再套重框，普通图标使用蓝色徽章，当前分区切换为金色并配左侧状态条，关闭按钮保留独立的红色悬停反馈。若上游改变设置 DOM，应先从已安装 DSH 的静态产物确认 `footArea`、`settingsArea`、`navCell`、`navIcon` 和 `close` 的新结构，再修改兼容层，不得凭截图猜类名。

## 修改与验证流程

开始修改前先阅读 `README.md`、相关源文件和现有测试，明确问题属于稳定视觉规则还是某个 DSH 版本的 DOM 适配。只修改解决当前问题所必需的文件；不要顺手引入框架、运行时依赖或与皮肤无关的容错。完成修改后运行 `npm test`，测试必须覆盖包契约、配置 schema、生命周期清理、稳定/兼容选择器边界、设置入口可见性、安全边界和分发完整性。任何失败都应直接修复根因，不得降低断言来掩盖回归；选择器数量上限只能在新增的每个选择器都有明确视觉职责时调整。

运行时验收只使用用户当前允许的 DSH 形态。当前机器策略禁止安装或启动公开全局 `dsh`、3080 服务以及临时 `npm exec dsh web`；不得为了截图或验收绕过这一限制。若用户允许在 DSH Desktop 或隔离内测 `dshd` 中验收，应检查设置入口、设置导航与关闭图标，同时覆盖新会话、会话切换、Chat/Trajectory、分支、模型、权限、文件树、Todo、窄侧栏和深色模式。验收证据至少记录目标版本、控制台错误数、皮肤自有节点数量和截图；没有运行时证据时必须明确写成“静态验证通过、运行时待验收”。

发布前运行 `npm pack --dry-run` 或在明确输出目录中执行 `npm pack`，核对 tarball 只含声明文件、只含一份背景、包名不冒用 `@deepseek-ai` 官方命名空间，并记录 SHA-256。DSH 仍处于可能破坏兼容性的预发布阶段，因此 `skin.json.compatibility.tested` 只能列出真实完成运行时验收的版本；仅通过静态源码比对的版本应留在维护记录中，不能冒充已测试版本。
